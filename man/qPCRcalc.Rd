% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/qPCRcalc.R
\name{qPCRcalc}
\alias{qPCRcalc}
\title{Calculate symbiont community proportions and symbiont:host signal from QuantStudio output}
\usage{
qPCRcalc(
  df,
  sdThreshold,
  QC = FALSE,
  ctCutoff = 40,
  singles = FALSE,
  returnAll = FALSE,
  rerunDetail = FALSE,
  ctWindow = 1
)
}
\arguments{
\item{df}{A data frame containing QuantStudio summary output. Expected columns
include \code{Sample.Name}, \code{File.Name}, and target-specific summary columns such as
\code{.reps}, \code{.CT.mean}, and \code{.CT.sd}.}

\item{sdThreshold}{Numeric threshold used to determine whether duplicate Ct values
pass replicate consistency QC.}

\item{QC}{Logical; if \code{TRUE}, return only the QC/rerun table.}

\item{ctCutoff}{Numeric Ct threshold for pass/fail filtering. Use \code{NA} to ignore
Ct cutoff and use only replicate count and SD criteria.}

\item{singles}{Logical; if \code{TRUE}, allow single replicates to pass under the
specified Ct rules.}

\item{returnAll}{Logical; if \code{TRUE}, return the processed output joined back to
the original input data.}

\item{rerunDetail}{Logical; if \code{TRUE} and \code{QC = TRUE}, return the detailed
target-level rerun table rather than the collapsed sample-level rerun table.}

\item{ctWindow}{Numeric Ct window above the minimum symbiont Ct within a sample
used to classify failed symbiont amplifications as relatively important for
rerun review.}
}
\value{
By default, a processed data frame containing symbiont proportions,
dominant/background symbiont identity, summed symbiont signal, and host-normalized
signal when host assays are present. If \code{QC = TRUE}, returns the rerun/QC table.
If \code{QC = TRUE} and \code{rerunDetail = TRUE}, returns the detailed target-level rerun
table. If \code{returnAll = TRUE}, returns the processed output joined back to the
original input data.
}
\description{
This function processes qPCR summary output exported from QuantStudio, applies
replicate-based quality control, calculates relative symbiont proportions, and,
when host assays are present, estimates total symbiont:host signal.
}
